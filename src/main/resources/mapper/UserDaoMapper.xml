<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.f.dao.UserDao">
    <insert id="insert">
        insert into employee(name, passwd, department_id, oa_position_id) values (#{name}, #{passwd}, #{departmentId}, #{oaPositionId})
    </insert>
</mapper>